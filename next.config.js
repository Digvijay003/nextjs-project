/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
    images: {
      domains: ["avatars.githubusercontent.com",'localhost','lh3.googleusercontent.com','images.pexels.com'],
    },
  };
  
  module.exports = nextConfig;